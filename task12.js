// ### Task-12
// Takes a string and returns it in uppercase.

function toUpperCaseString(str) {
    return str.toUpperCase();
}
  
console.log(toUpperCaseString("hello world")); // Output: "HELLO WORLD"
  